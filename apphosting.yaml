# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1

# This rewrites all requests to the Next.js server
headers:
  - host: 'login-67c98.web.app'
    source: '/(.*)'
    destination: 'https://login-67c98--q7k3ekq9.web.app'

# This is a legacy feature that should not be used for Next.js apps
# We are removing it.
#
# rewrites:
# - source: /
#   destination: /index.html
